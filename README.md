REST API using Datamapper & Sinatra to manipulate courses and enrolments in a Moodle database.

curl -i -H "Accept: application/json" -X POST -d "{\"fullname\":\"foo\", \"shortname\":\"f\"}" http://localhost:9292/courses